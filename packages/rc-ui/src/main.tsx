import React from 'react';
import ReactDOM from 'react-dom';
import Button from './Button';

ReactDOM.render(
  <React.StrictMode>
    <Button title="vite-componnets" />
  </React.StrictMode>,
  document.getElementById('root'),
);
